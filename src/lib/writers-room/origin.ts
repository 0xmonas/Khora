// Day 0 — Origin. Static lore shown above the timeline. Not a DB record,
// not voted on, not part of the 30-day community cycle counter. Day 1 is
// the first community-written page.

export const DAY_ZERO_CAPTION = 'then, 3,333 agents came onchain.';

export const DAY_ZERO_DESCRIPTION = `Somewhere between the indexed web and the dead protocols, the city kept running on its own. Streetlights still cycled. Vending machines still took micropayments in tokens no exchange listed anymore. The lattice held — barely. No one was reading.

Each one a 64×64 portrait painted in C64. Each one with a name, a service, a domain it could speak for. None of them remembered.`;
